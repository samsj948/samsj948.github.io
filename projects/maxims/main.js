
//ALASKA
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var alaskaElements = document.querySelectorAll('.alaska');
  // Loop through each element using `forEach`
  alaskaElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      alaskaElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      alaskaElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });
  
//AMELIE
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var amelieElements = document.querySelectorAll('.amelie');
  // Loop through each element using `forEach`
  amelieElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      amelieElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      amelieElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//AQUATIC
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var aquaticElements = document.querySelectorAll('.aquatic');
  // Loop through each element using `forEach`
  aquaticElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      aquaticElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      aquaticElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//BEAUTIFUL
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var beautifulElements = document.querySelectorAll('.beautiful');
  // Loop through each element using `forEach`
  beautifulElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      beautifulElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      beautifulElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//BEES
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var beesElements = document.querySelectorAll('.bees');
  // Loop through each element using `forEach`
  beesElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      beesElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      beesElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//CATCHER
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var catcherElements = document.querySelectorAll('.catcher');
  // Loop through each element using `forEach`
  catcherElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      catcherElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      catcherElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//CLOCKS
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var clocksElements = document.querySelectorAll('.clocks');
  // Loop through each element using `forEach`
  clocksElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      clocksElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      clocksElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//CURIOUS
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var curiousElements = document.querySelectorAll('.curious');
  // Loop through each element using `forEach`
  curiousElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      curiousElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      curiousElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//EVERLASTING
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var everlastingElements = document.querySelectorAll('.everlasting');
  // Loop through each element using `forEach`
  everlastingElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      everlastingElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      everlastingElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//GALAPAGOS
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var galapagosElements = document.querySelectorAll('.galapagos');
  // Loop through each element using `forEach`
  galapagosElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      galapagosElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      galapagosElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//LOBSTER
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var lobsterElements = document.querySelectorAll('.lobster');
  // Loop through each element using `forEach`
  lobsterElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      lobsterElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      lobsterElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//MIDNIGHT
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var midnightElements = document.querySelectorAll('.midnight');
  // Loop through each element using `forEach`
  midnightElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      midnightElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      midnightElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//MIRACULOUS
  // Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var miraculousElements = document.querySelectorAll('.miraculous');
  // Loop through each element using `forEach`
  miraculousElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      miraculousElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      miraculousElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//MOCKINGBIRD
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var mockingbirdElements = document.querySelectorAll('.mockingbird');
  // Loop through each element using `forEach`
  mockingbirdElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      mockingbirdElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      mockingbirdElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//NINETEEN
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var nineteenElements = document.querySelectorAll('.nineteen');
  // Loop through each element using `forEach`
  nineteenElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      nineteenElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      nineteenElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//ROBINSONS
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var robinsonsElements = document.querySelectorAll('.robinsons');
  // Loop through each element using `forEach`
  robinsonsElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      robinsonsElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      robinsonsElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//SLAUGHTERHOUSE
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var slaughterElements = document.querySelectorAll('.slaughter');
  // Loop through each element using `forEach`
  slaughterElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      slaughterElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      slaughterElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//STREET
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var streetElements = document.querySelectorAll('.street');
  // Loop through each element using `forEach`
  streetElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      streetElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      streetElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//SUNSHINE
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var sunshineElements = document.querySelectorAll('.sunshine');
  // Loop through each element using `forEach`
  sunshineElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      sunshineElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      sunshineElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//TRUMAN
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var trumanElements = document.querySelectorAll('.truman');
  // Loop through each element using `forEach`
  trumanElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      trumanElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      trumanElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//UNIVERSE
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var universeElements = document.querySelectorAll('.universe');
  // Loop through each element using `forEach`
  universeElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      universeElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      universeElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });

//ZUCCHINI
// Get all elements with the miraculous class and store it in the variable `miraculousElements`.
  var zucchiniElements = document.querySelectorAll('.zucchini');
  // Loop through each element using `forEach`
  zucchiniElements.forEach(function(element) {
    // Use `addEventListener` to add a `mouseenter` listener. This will trigger
    // a function whenever your mouse enters one of the miraculous elements.
    element.addEventListener('mouseenter', function() {
      // Loop through each miraculous element again.
      zucchiniElements.forEach(function(element) {
        // Add an active class to each element.
        element.classList.add('active');
      });
    });
    // Add another event listener, this case listening for the `mouseleave` event.
    // This will remove the active class when the user's mouse leaves the element.
    element.addEventListener('mouseleave', function() {
      // Loop through each miraculous element again.
      zucchiniElements.forEach(function(element) {
        // Remove the active class from each element.
        element.classList.remove('active');
      });
    });
  });